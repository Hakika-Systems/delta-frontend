# Use npm registry
registry=https://registry.npmjs.org/

# Disable optional dependencies
optional=false

# Force use of package-lock.json
package-lock=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true 